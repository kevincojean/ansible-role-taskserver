---

- name: Create taskd group
  become: true
  group:
    name: "{{ taskd_group }}"
    state: present

- name: Create taskd user
  become: true
  user:
    name: "{{ taskd_user }}"
    group: "{{ taskd_group }}"
    state: present
    createhome: false
